# Core ML
torch>=2.0
transformers>=4.40
sentence-transformers>=2.6
datasets>=2.18
bitlinear>=0.5

# Config + I/O
pydantic>=2.5
pyyaml>=6.0
numpy>=1.24

# Eval metrics (Phase 4 — kept here so one venv covers all phases)
evaluate>=0.4
scikit-learn>=1.3

# Tracking
wandb>=0.16
