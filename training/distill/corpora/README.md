# corpora

Hand-curated retrieval eval data for `evaluate.py` Task 3. One JSON object per line: `{"query": "...", "match": "..."}`.

Remote datasets (MS MARCO, STS-B) aren't here — see [../configs/eval.yaml](../configs/eval.yaml).
